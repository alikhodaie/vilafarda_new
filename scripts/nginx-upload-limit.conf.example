# نمونه برای سایت vilafarda — داخل بلوک server { } در nginx
# بعد از ویرایش: sudo nginx -t && sudo systemctl reload nginx

client_max_body_size 128M;

# اگر از php-fpm استفاده می‌کنید، معمولاً کافی است.
# PHP: post_max_size و upload_max_filesize در php.ini (مثلاً 128M)
